React native is just like react. There are components and state.
View component is like a div

Expo is framework on top of react native that makes development and setup for react native apps simpler.

-- start the app
npm start

--install library for location
npx expo install expo-location
npx expo install expo-background-fetch
npx expo install expo-sqlite
npx expo install expo-file-system expo-asset


-- background task running
https://docs.expo.dev/versions/latest/sdk/background-fetch/?redirected

-- there is some issue in background task in ios

